# CLAUDE.md - AI Assistant Guide for LED_VR

## Project Overview

LED_VR is a project combining LED (Light Emitting Diode) hardware with VR (Virtual Reality) technology. The repository is in its initial stage of development.

## Repository Structure

```
LED_VR/
├── README.md       # Project readme (minimal)
├── CLAUDE.md       # This file - AI assistant guide
```

The project is freshly initialized. No source code, build system, or tooling has been set up yet.

## Development Environment

- **Platform:** Linux
- **Version Control:** Git
- **Remote:** GitHub (holica49/LED_VR)
- **Default Branch:** `master`

## Git Conventions

- The default branch is `master` (not `main`).
- Feature branches should use descriptive names prefixed with the feature area (e.g., `feature/led-driver`, `fix/vr-latency`).
- Write clear, imperative commit messages (e.g., "Add LED strip controller module").

## Current State

This is a greenfield project. When contributing:

1. **No build system configured yet** -- when one is added, document the build commands here.
2. **No test framework configured yet** -- when one is added, document how to run tests here.
3. **No linter or formatter configured yet** -- when one is added, document the commands here.
4. **No CI/CD pipeline configured yet** -- when one is added, document the pipeline here.
5. **No dependencies declared yet** -- when a package manager is chosen, document install steps here.

## Guidelines for AI Assistants

- Read existing code before proposing changes.
- Keep changes minimal and focused on the task at hand.
- Do not add unnecessary abstractions, comments, or documentation beyond what is requested.
- When the project grows, update this file to reflect new structure, commands, and conventions.
- Prefer editing existing files over creating new ones unless the task requires it.

## Quick Reference (to be filled in as the project develops)

| Task | Command |
|------|---------|
| Install dependencies | TBD |
| Build | TBD |
| Run tests | TBD |
| Lint | TBD |
| Format | TBD |
